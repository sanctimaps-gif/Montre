{
  "name": "@montre/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API de l'application : comptes, activites, coaching, synchronisation Strava.",
  "scripts": {
    "dev": "npm run build -w @montre/core && node --experimental-strip-types --watch src/index.ts",
    "start": "node --experimental-strip-types src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@montre/core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
